services:
  - type: web
    name: university-spring-backend
    env: java
    buildCommand: ./gradlew bootJar
    startCommand: java -jar build/libs/UniversityDepartmentSystem-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17